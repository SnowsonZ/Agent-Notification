# 发布包（--standalone）内置的纯 Python 依赖。2026-09-21 起最低运行时为 3.11
# （tomllib、websockets 17 均要求 ≥3.11），与开发环境 requirements.txt 同版本锁定，
# 不再为 macOS 自带 python3 3.9 维护降级 pin。
iterm2==2.23
protobuf==7.36.1
websockets==17.1
