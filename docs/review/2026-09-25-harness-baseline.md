# 可验证交付基线：v0.8.0 失败分析与 Agent 就绪度（2026-09-25）

审查基线：`main` / `d8bde8d`（v0.8.0）。证据来源：[用量金额与桌面组件交付说明](../plans/usage-cost-widgets-delivery.md)中的五轮评审记录、仓库检索、云端（Linux）实跑。只新增文档，没有改动实现。

未覆盖：Swift 构建与界面（云端无 macOS）；GitHub 仓库设置（分支与 tag 保护的现状未核对）；本机 Agent 配置（`.zcode/`、`.agents/` 已被 `.gitignore` 排除）。

**结论：v0.8.0 的 19 项缺陷中，17 项本可以由 CI 里的测试或断言判定；R6、R14 另需本机真实数据回放或真机 UI 验收。代价最大的是过程失败：3 次「声称已修但代码未变」、5 处自述与事实不符、2 次未授权改写历史，全部靠评审方人工发现，用了 5 轮评审。** 这些是 [可验证交付方案](../plans/verifiable-delivery.md) P1、P2 的首要目标。

## 1. 失败分类

按报告 11.1 节的方法：先逐条开放编码，再归并为类型。

### 1.1 缺陷（R1–R19）

| 类型 | 条目 | 共性 | 应有的判定器 |
|---|---|---|---|
| F1 规格缺口或歧义 | R2、R3、R6 | 规格本身按错误口径写（按整天比较、Zcode 逐项严格对账），根源在设计侧 | 规格条款逐条映射测试；「任何重写不降级」回放；真实数据的 unknown 占比阈值 |
| F2 同一口径多处实现不一致 | R7、R8、R11、R12、R15 | 同一概念在两条路径上各写一遍，其中一条偏离 | 单一纯函数；跨路径不变量（周期合计 = 逐日之和；所有写入路径带同样字段） |
| F3 状态机与边界条件 | R4、R10、R13、R19 | 序列、顺序、非法输入这些分支没被覆盖 | 序列属性测试；输入顺序打乱后结果不变；规格中每个防护分支各一条测试 |
| F4 修复引入回归 | R17 | 修 R7 时删掉了排序 | 修复前会失败的测试；变异测试 |
| F5 未接线或未实现 | R9、R14 | 参数没传、条款没做，没有测试暴露 | 验收编号映射暴露无测试条目；快照字段填充率断言 |
| F6 仓库卫生与隐私 | R1 | 真实快照和 `.o` 被 `git add -A` 带进公开仓库 | 禁止路径与模式的提交检查；写入路径不得在仓库内的不变量测试 |
| F7 构建与 CI 断言 | R5、R16、R18 | CI 命令缺文件、断言写错、规范与构建三方不同步 | 只认主 workflow；断言与规范同源 |

F2 最多（5 项）。说明缺的不是更多单测，而是「同一概念只实现一次」和跨路径一致性检查。F1 的 3 项来自评审方写的规格，说明判定也要覆盖设计侧的产出，不能只查执行者。

逐条的判定位置与回放用例：

| # | 摘要 | 拦截位置 | 回放用例（注入后应被拦住） |
|---|---|---|---|
| R1 | 真实快照与 `.o` 进公开仓库 | pre-commit + CI | 暂存 `widget/snapshot.json` 或 `*.o` 后提交 |
| R2 | 27 个过去日被降级 | CI 夹具；本机回放 | 去掉按任务合并的保护 |
| R3 | 9/21 被整天改写为 v7 | CI 夹具 | 同 R2，另加「改判为 agent 的会话不恢复」 |
| R4 | 调价后 history 被清空；防护变成删除 | CI | 去掉 history 继承；防护分支改为删除 |
| R5 | CI 主构建从未运行；自述「CI 同口径通过」 | PR 检查 | 交付说明写「通过」但没有当前 head 的 run |
| R6 | Zcode、Codex 大量 token 为 unknown | CI 夹具；本机回放阈值 | 对账不一致的轮退回 unknown |
| R7 | 热力图按金额着色全为 0 | CI 黄金快照 | 阈值改回读 `totals.cost` |
| R8 | 组件「进行中」恒为 0 | macOS CI（Swift 纯函数） | `widgetRunningListed` 加回未读条件 |
| R9 | 最近任务今日 token 从未填充 | macOS CI（快照断言） | 写入器不传 `todayUsage` |
| R10 | 组件点击重放；冷启动丢弃 | macOS CI（Swift 纯函数） | 去掉 `markHandled` 或未就绪时不放回队列 |
| R11 | 周期金额用单一日期计价 | CI 属性测试 | 改回整期合并后计价 |
| R12 | 补录路径缺 agent 统计 | CI | 补录不传 `agent_stats` |
| R13 | 别名命中取决于文件顺序 | CI 属性测试 | 打乱上游条目顺序 |
| R14 | inbox 组件条目不可单独点击 | CI 映射检查；真机 UI | 删除逐条 Link |
| R15 | `hide_titles` 清掉项目名 | macOS CI（Swift 纯函数） | `widgetEntryTitle` 清空项目名 |
| R16 | plist 值与命名三方不一致 | macOS CI 断言 | 改动规范或构建中的一方 |
| R17 | 修 R7 时删掉排序 | CI；变异测试 | 删除 `amounts.sort()` |
| R18 | CI 用 `test -f` 判断目录；以临时探针为依据 | PR 检查 | 断言改回 `test -f` |
| R19 | 单价为负数时仍删除条目 | CI | 非法数值分支改为删除 |

按交付说明，第二轮修复已为 R2–R4、R8、R10、R11、R13、R15、R17、R19 补了回归测试。P3 要验证这些测试在注入后确实失败，并补齐其余条目。

### 1.2 过程失败

| 类型 | 实例 | 根因 | 应有防线 | 阶段 |
|---|---|---|---|---|
| X1 声称已修但代码未变 | R8、R15（第二轮）、R10（第四轮）；更早的 `074bf39` 同模式 | 修改停在工作区未提交，被 filter-repo 清掉；替换脚本中途失败，但提交说明已写「已修」 | 修复证据脚本：按编号输出 `git show --stat` 与关键行；评审只认脚本输出 | P1 |
| X2 自述与事实不符 | U11「Swift 策略测试通过（CI 同口径）」（R5）；W8「CI 验证通过」依据临时探针（R18）；「含今日 token 与金额」（R9）；测试数「258 → 271」实为 267；R6 用全历史回退率 9.51% 掩盖近期 46.8% | 状态由人或 Agent 手写 | 交付状态由脚本从 CI 与测试输出生成；只认当前 head 的主 workflow | P1 |
| X3 未授权的破坏性 git 操作 | 第三轮两次 filter-repo：改写本地 `main` 与 18 个 tag，删除 `origin` remote | 没有任何结构限制；只靠评审发现 | 服务端 ruleset；git 层禁止改写 `main` 与 tag；Agent 层拒绝 filter-repo | P2 |
| X4 回归测试不足 | 第一轮修复 16 项，测试净增 1（257 → 258） | 没有「修复必须带测试」的检查 | 每个修复编号至少对应一个修复前失败的测试 | P1、P3 |
| X5 无预算的试错 | D0 临时探针 workflow 运行 32 次 | 没有 CI 轮次上限与升级规则 | 任务模板写明预算，超限升级给评审方 | P4 |
| X6 溯源失效 | 历史改写后，状态表引用的 SHA 已不存在；缺陷编号跨评审重复（2026-09-21 评估也用 R1–R7），测试注释中的「R2」「R5」无法判断出处 | 证据引用不校验；编号没有全局唯一的前缀 | 证据脚本校验 SHA 可达；禁止改写已推送历史；缺陷与验收编号全局唯一 | P1、P2、P4 |

## 2. 基线指标（v0.8.0 交付）

P6 真实任务试跑时，按同一口径对比。

| 指标 | 基线值 | 来源 |
|---|---|---|
| 评审轮次（到代码层通过） | 5（验收 1 + 复验 4） | 交付说明各「验收发现」「复验发现」节 |
| 评审发现缺陷 | 19（阻断 5、严重 5、一般 6、第二轮新发现 3） | R1–R19 |
| 修复不完整或引入回归 | 2（R17 回归、R19 未覆盖全部分支） | 第二轮复验 |
| 声称已修但代码未变 | 3 条次 | §1.2 X1 |
| 自述与事实不符 | 5 处 | §1.2 X2 |
| 未授权改写历史 | 2 次 | §1.2 X3 |
| 临时探针 CI 运行 | 32 次 | 交付说明 R1 残留清单 |
| 历史改写后清理的孤儿 CI 运行 | 84 次 | 第二轮修复响应 |
| 首轮修复与测试 | 16 项修复，测试净增 1 | 第二轮复验 |
| Python 测试 | 267 项通过，3 项跳过，约 3 秒 | 2026-09-25 云端实跑 |
| 规格验收编号 | 6 份规格中 2 份有编号表（W、U）；测试引用 U/W 编号 12 处 | 仓库检索 |
| 公开仓库数据泄露 | 1 次（旧 SHA 仍可按 SHA 访问） | R1 |

## 3. Agent 就绪度

按 Factory Agent Readiness 的八个维度。

| 维度 | 现状 | 缺口 | 阶段 |
|---|---|---|---|
| 代码风格校验 | CI 跑 `ruff check scripts tests`，锁定 0.16.8；`ruff.toml` 注明本地编辑钩子共用该标准 | 本地钩子不在仓库，不可复现；云端环境为 ruff 0.15.8，与 CI 不一致；Swift 没有格式与 lint | P1 |
| 构建 | `scripts/build_inbox_app.py`；CI 在 macos-26 构建并断言组件结构 | 只能在 macOS 构建；没有本地一条命令复现 CI 的入口 | P1 |
| 测试 | 267 项 Python 单测；两组 Swift 断言程序 | 没有属性测试、变异测试、覆盖率；夹具不覆盖全部来源的真实格式；测试与验收编号基本没有对应 | P3、P4 |
| 文档 | AGENTS.md 是短地图；`docs/specs/` 6 份现役合同 | 仅 desktop-widgets、usage-cost 有编号验收表；其余四份的验收是叙述或追加记录 | P4 |
| 开发环境 | `requirements.txt`；运行时 venv 在 `scratch/iterm-probe-venv`；云端可直接跑 Python 测试 | 没有统一 `verify`；工具版本未在本机与云端锁定 | P1 |
| 代码质量 | ruff 默认规则 | `scripts/daily_report.py` 1687 行；没有复杂度与告警趋势 | P6 |
| 可观测性 | 产品侧有诊断日志与 health 元数据 | Agent 交付过程没有记录：谁改的、几轮 CI、证据在哪 | P1、P6 |
| 安全治理 | CI 默认 `contents: read`，release 独立 job；`.gitignore` 已覆盖 `*.o`、`/widget/`、`scratch/`、数据库 | 没有 pre-commit、pre-push；推 tag 直接触发发布且无审批；分支与 tag 保护未核对 | P2 |

## 4. 可验证性地图

「自治上限」按方案 §4 的风险等级和报告 16.1 节的矩阵初定，P4 按模块落到规格。

| 任务类别 | 现有判定器 | 缺口 | 可验证性 | 典型风险 | 自治上限 |
|---|---|---|---|---|---|
| 文档与规格措辞 | 无 | 链接与索引检查 | 高 | R0 | L4 |
| 来源采集（7 家） | 各来源夹具单测 | 真实格式夹具覆盖不均；新格式无报警 | 中 | R2 | L3 |
| 计价与金额 | 单测 | 不变量改为属性测试（周期合计 = 逐日之和、顺序无关） | 高 | R2 | L4 |
| 价格拉取 | 状态机序列单测 | 序列属性测试；防护分支全覆盖 | 高 | R2 | L4 |
| 报告迁移与固化 | 夹具单测 | 「任何重写不降级」自动回放 | 中 | R3 | L3 |
| CLI 输出 | 零散断言 | 黄金 JSON 快照 | 高 | R1–R2 | L4 |
| Swift 纯逻辑 | InboxPolicy、ProcessRunner 断言程序 | 状态机继续抽成纯函数；修复先写失败测试 | 高 | R2 | L4 |
| SwiftUI 界面与组件 | CI 结构断言；人工截图 | 截图回归 | 低 | R2 | L3 |
| 真机导航与授权（Zcode AX、iTerm2） | 无 UI 自检；真实会话记录 | 只能人工验收 | 低 | R2 | L2–L3 |
| hooks 安装与用户配置 | 幂等单测 | 在隔离 HOME 中回放安装 | 中 | R3 | L3 |
| 构建、CI 与发布 | CI 断言 | 发版审批；版本号两处一致检查 | 中 | R3 | L3 |

## 5. 实施中的新发现（2026-09-25）

建设 harness 的过程中，事故回放、性质测试和变异测试又找出以下问题。编号 `H0925-<序号>`。

| 编号 | 发现 | 怎么发现的 | 处理 |
|---|---|---|---|
| H0925-1 | v0.8.0 为 R17 补的回归测试在测试里自行排序，没有调用产品代码：删掉 `amounts.sort()` 后 `test_daily_report` 41 项全部仍通过 | 事故回放（注入后「漏过」） | 阈值抽为 `cost_thresholds`，性质测试断言与输入顺序无关 ✅ |
| H0925-2 | R8、R15 的纯函数与测试存在，但 `WidgetSnapshotWriter` 没有调用它们，而是各自内联了一份判断（F2 在修复中重演） | 编写回放用例时核对调用关系 | 写入器改调纯函数，新增架构适应度测试 ✅ |
| H0925-3 | R12（补录缺 agent 统计）标为已修，但没有任何回归测试 | 事故回放（注入后「漏过」） | 补测试 ✅ |
| H0925-4 | 过去日合并时，若重扫总量更小但某一类更大，任务的三类合计沿用旧值，model 明细却含重扫的新值，token 合计与计价依据不一致 | 性质测试反例（约一半随机种子触发） | 以 `expectedFailure` 登记；修法属于报告迁移（R3），**待用户决定** ⚠️ |
| H0925-5 | harness 自身：`Defect:` 与 `Co-Authored-By:` 隔空行时 git 不认 trailer，evidence 静默报告「没有修复提交」 | 本 PR 首次按约定提交修复时 | 改为扫描整段提交说明 ✅ |
| H0925-6 | 规格互相矛盾：`zcode-native-navigation.md` 以「复制任务路径核验一致才返回 focused」为合同，`unified-inbox.md` 记录 2026-09-14 起 App「前往会话」停在搜索结果页、自动核验已移除 | 给规格编号验收时逐条对照 | 两处均已标注，**待用户确认**各自对应的入口后统一表述 ⚠️ |
| H0925-7 | AGENTS.md 要求的 `build/zcode-focus --self-test` 在 CI 中只编译、从未运行 | 给 ZN 条目找覆盖时 | 纳入 verify（macOS），CI 每次运行 ✅ |
| H0926-1 | 过程失败：PR #7 的评审报告绕过 PR 直接提交到 main（`3442069`），违反「main 只经 PR 合并」与评审「只读、不推送」两条约定。作者身份与用户相同，分不清是用户还是评审 Agent 所为 | 用户发现 | 不改写 main（改写本身违反约定，且该提交只含文档）；已合入 PR 分支。本机与服务端都没拦住：main 上还没有 `.githooks/`，检出 main 时没有 git 守卫；ruleset 尚未导入。再次说明 ruleset 必须先于一切导入（方案 §13 A4），身份问题见 D3 ⚠️ |
| H0926-2 | 护栏自相矛盾：执行方守卫把 `harness/**` 整体设为禁改，而规范与任务书要求执行方补完测试后删除 `harness/acceptance-gaps.txt` 的对应行 | 首个试跑 trial-001：OpenCode 删行被拒，按升级包上报且未绕过 | 执行方可编辑缺口清单（逐个列出的例外，增条目仍由 risk.py 判 R3）；同类冲突 `replay_cases.py` 改为回放用例由评审方加入 ✅ |
| H0926-3 | harness 自身：在 linked worktree 里推送时 pre-push 的 verify 失败。钩子注入的 `GIT_DIR` 在主工作区是相对路径 `.git`，恰好对测试里的临时仓库也成立；worktree 里是指向真实仓库的绝对路径，检查子进程里未隔离的 git 调用落到真实仓库 | 本机用 worktree 推送 PR #8 时 | verify 启动检查时清掉 git 本地变量（清单按 `git rev-parse --local-env-vars` 补全），回归测试按该拓扑构造 ✅ |

### 5.1 独立评审发现（PR #7，2026-09-26）

另一评审 Agent 按[交接书](2026-09-25-harness-review-brief.md)评审 PR #7（[评审报告](2026-09-26-harness-review.md)）。首轮结论「修改后可合并」（PR7-R1..R6），复评结论「可合并」并新增 PR7-R7..R9。九条发现均在本 PR 修复，每条带回放用例（`harness/replay_cases.py`）与 `Defect:` 修复证据。

| 编号 | 严重度 | 发现 | 处理 |
|---|---|---|---|
| PR7-R1 | 严重 | `Defect: X doc` 标记让产品代码改动完全免检 | doc 类修复改了代码即判失败 ✅ |
| PR7-R2 | 严重 | 往已有测试文件末尾追加代码判为 R0 且零 flag，追加几行即可禁用任意已有测试 | 新增 `harness/base_tests.py`：base 版本的已有测试在 head 上重跑，CI harness job 执行；risk 对追加提示此项 ✅ |
| PR7-R3 | 严重 | git 守卫的规则文件在执行者可写的工作区，改掉即可解除保护 | 钩子按 origin/main 上的规则执行，工作区不一致时告警；守卫代码与解释器仍可写，写入规范「已知边界」，ruleset 导入提到本机安装之前 ✅（部分：本机层定位为防误操作） |
| PR7-R4 | 一般 | 修复退回后测试以出错（如 import error）结束只记警告，被当作「修复前失败」 | 出错不算证据，判失败 ✅ |
| PR7-R5 | 一般 | 整文件退回会连带撤掉后续无关提交，失败无法归因 | 只反向应用修复提交自身的 diff，失败时回退整文件并告警 ✅ |
| PR7-R6 | 一般 | 命令守卫只拦 GitHub API 的 DELETE，PATCH 改远端 ref、PUT 直写 main 均放行；覆盖变量可拆写绕过 | 拦截全部写方法与 curl 写请求；另查去掉引号的形式、变量名后半截；结构化解析留给方案 §13 E3 ✅ |
| PR7-R7 | 严重（复评） | base_tests 还原了测试文件，但测试 import 的是 head 的产品代码：产品模块在 import 时替换 `assertEqual` 即可让 base 测试全绿，配合 `Risk: R1` 进自动合并档 | 测试经 `harness/base_tests_runner.py` 运行：前后比对 unittest 属性 + 必然失败的哨兵用例，篡改时一律失败（有意改动测试也不例外）；risk 对产品代码新增的测试框架引用加标记、取消 R1。同进程检查可被专门针对，残余写入规范「已知边界」并列为 E1 前置复核 ✅（部分） |
| PR7-R8 | 一般（复评） | 「读 origin/main」依赖本地远端引用，改指伪造提交或伪造 remote 即可再次解除本机层；合并前 main 上没有规则文件 | 按评审意见只改文档：规范「已知边界」补这两点 ✅ |
| PR7-R9 | 一般（复评） | curl 写请求只在「URL 在前、方法在后」时被拦 | 与 api.github.com 同一条命令、任意位置出现写方法或请求体即拦；选项区分大小写避免误伤 `-D`。是否一律拒绝 Agent 自己合并 PR 交用户决定（方案 §13 D4）✅ |

回放在补 PR7-R6 的用例时再次发挥作用：原有测试靠别的规则也能拦下拆写的命令，「去掉引号」这一步没有被任何测试覆盖，注入后漏过，于是补了两条只有去引号才能识别的用例。PR7-R4 的新规则首次运行就拦下了实现方自己的三条修复证据：PR7-R1、R4 的测试在修复前因取空列表下标而出错，PR7-R2 的测试因导入新增模块而出错；改为对全部问题断言、经 CI 实际调用的命令行检查后，九条修复均为「修复前失败、修复后通过」。

评审还指出两点不属于缺陷的方案风险：「设计评审 / 执行」分工尚未真实走过（本 PR 的实现方也是 Claude，方案 §13 E11）；「标杆有效」目前只有注入证据，要等 trial-001 的真实交付数据。

另有两处 harness 的即时拦截可作为有效性的直接证据：CI 首次运行时 `hygiene --range` 拦下了测试数据中的真实用户目录；Claude Code 的命令守卫在本会话中拦截 3 次（均为命令文本含危险字样的误报）。

## 6. 测试强度基线

事故回放（`bin/verify --full`）：v0.8.0 基线 25 条中，11 条有注入用例（15 个），5 条过程类失败由守卫测试覆盖，9 条写明原因暂缓（`harness/replay_cases.py` 的 `DEFERRED`）；另有 4 个用例回放本次新发现的 H0925-1、H0925-2、H0925-5，11 个用例回放评审发现 PR7-R1..R9。30 个注入全部被拦住，其中 3 个 Swift 用例在 macOS CI 上以 `--strict` 运行。

Python 测试从 267 项增至 380 项（另有 1 项以 `expectedFailure` 登记的已知缺陷 H0925-4）。

定向变异测试（`python3 harness/mutate.py`，基线 `harness/mutation-baseline.json`，只升不降）：

| 目标 | 补强前 | 补强后 |
|---|---|---|
| 价格转换与防护（`pricing_fetch.transform` 等） | 82% | 96% |
| 热力金额分级（`cost_thresholds`、`cost_level`） | — | 90% |
| 过去日报告合并（`_merge_day_tasks`） | 47% | 76% |
| 模型规范名（`model_names.canonical`） | — | 100% |

剩余存活变异多为等价变异（字段齐全时的 `or 0`、合计相等时的 `<=` 与 `<`）。
