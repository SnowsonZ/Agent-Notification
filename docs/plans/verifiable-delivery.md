# 可验证交付：以本项目为标杆的全链路 harness

状态：**P0–P5 完成，P6 的工具与试跑任务就绪，真实试跑待执行方执行**（2026-09-25）。基线评审见 [2026-09-25 可验证交付基线](../review/2026-09-25-harness-baseline.md)，现役规则与命令见 [可验证交付 harness](../specs/delivery-harness.md)。

理论来源：用户的调研报告《低人工干预下 AI Agent 持续高质量交付：理论与全链路最佳实践》（2026-09-23，Claude 文档，未入库）。本文只保留落到本仓库的部分；报告中面向大团队的做法按单人项目裁剪。

## 1. 目标

把本项目每一类改动的完成判断，从「Agent 说做完了、人去复验」改为「开工前就有可执行的判定，完工后由 Agent 之外的机器重跑判定并留下证据」，并用 v0.8.0 的真实事故证明这套机制有效。

标杆的含义：**有效**（有数据证明）+ **可复制**（模板、脚本和文档可被其他项目照搬）。

## 2. 「可验证」的操作定义

一个任务算「已转成可验证问题」，要同时满足五条：

1. **验收有编号**：每条是命令或测试，并标注证据类型（单测 / 夹具 / 真实数据回放 / CI 断言 / 真机 UI）。AGENTS.md「验证边界」已要求区分，这里把它变成机器检查。
2. **判定在 Agent 之外**：以当前 head 的主 workflow run 为准，不采信自述。
3. **判定器本身被检验**：修复类改动的测试必须在修复前失败；关键模块测变异得分。
4. **执行者改不了判定器**：已有测试、CI、hooks、验收映射的修改或删除自动升级给评审方；新增测试不受限。
5. **证据由脚本生成**：diff 文件列表、关键行、CI 链接由脚本产出，不手写。

第 1 条做不到的（真机导航、系统授权、视觉体验）明确标为人工验收，列入清单，不假装自动化。

## 3. 角色（2026-09-25 用户决定）

| 角色 | 由谁担任 | 职责 | 不做什么 |
|---|---|---|---|
| 设计与评审 | Codex、Claude Code | 写规格与验收编号、计划与风险等级；评审（只认证据包）；执行者卡住时做顾问 | 不替执行者提交实现代码 |
| 执行 | Zcode、OpenCode、Pi | 实现、补测试、本地构建与安装开发包、推分支、开 PR | 不改已有判定器；不打 tag；不改写已推送历史 |
| 人 | 用户 | 意图与优先级；R2 以上的合并；版本号确认与发版审批；真机 UI 验收 | 不逐条审批命令、不逐行审代码 |

生成与评审由不同工具、不同模型承担，本身就是报告 2.4 节的「异构独立评审」。

## 4. 风险等级与自治（用户选定 L4）

风险等级由 CI 按改动路径判定，不由执行者自报。

| 等级 | 本项目的典型改动 | 合并方式 |
|---|---|---|
| R0 | 文档、新增测试、lint 修复 | 门禁全绿即自动合并 |
| R1 | 行为不变的重构（判据：已有测试零修改、黄金快照零差异） | 自动合并 + 抽样审计 |
| R2 | 功能改动、规格改动、界面改动 | 评审方评审 + 用户看证据包后合并 |
| R3 | 报告迁移与重写、隐私相关（快照、`hide_titles`、诊断日志）、hooks 安装（改用户配置）、CI 与发布流程、护栏本身、git 历史 | 必须由用户批准 |

## 5. 护栏分层

Agent 层取决于各家宿主（最初调研认为 Zcode 没有工具调用 hook，见 [推送通道调研](../research/2026-09-20-push-channels-per-source.md)；2026-09-26 核实 Zcode 0.16.9 已支持 `PreToolUse`），且 Zcode、Pi 的项目钩子都要用户信任后才生效，所以硬边界不能放在 Agent 层。

| 层 | 机制 | 能否被绕过 | 作用 |
|---|---|---|---|
| 服务端 | GitHub ruleset：`main` 禁 force push 与删除、必须经 PR 与 `build` 检查；`v*` tag 禁移动与删除；release job 走需要用户审批的 environment | 本机 Agent 无法绕过 | 硬边界 |
| git | 仓库内 `core.hooksPath`：pre-commit（禁止路径与隐私模式）、pre-push（禁推 `main` 与 tag）、reference-transaction（禁本地改写 `main` 与 tag，待实测） | `--no-verify` 等可绕过 | 与工具无关的快速反馈、防误操作 |
| Agent | Claude Code / Codex 的 PreToolUse；OpenCode 插件、Pi 扩展、Zcode 工作区钩子（后两者需用户信任） | 各家不同 | 最早的反馈，锦上添花 |

未决：所有 Agent 与用户共用同一个 GitHub 身份时，服务端无法区分操作者（例如 R2 改动能否由执行者自行合并）。P2 决定是否给执行者单独身份（机器账号或 GitHub App）。

## 6. 全流程落点

| 环节 | 本项目要建的判定 | 阶段 |
|---|---|---|
| 需求与规格 | 全部规格的验收条目编号并标证据类型；CI 校验「可自动化编号 ↔ 测试名」映射 | P4 |
| 上下文 | 一条 `verify` 命令，本机、云端、CI 同口径；工具版本锁定 | P1 |
| 规划 | 任务模板与计划模板（终态、非目标、每步验证方式、风险等级） | P4 |
| 执行 | §5 三层护栏 | P2 |
| 验证 | 金额不变量属性测试、CLI 黄金快照、七个来源的真实格式夹具、「任何重写不降级」回放、Swift 纯逻辑单测、关键模块变异测试 | P3 |
| 集成发布 | 只走 PR；CI 链接是唯一通过凭证；按路径判定风险等级；发版审批 | P1、P2 |
| 学习改进 | 失败分类法；评审发现逐条转成回归用例或自动检查 | P0、P6 |
| 度量 | 复验轮数、声称已修次数、自述不实次数、CI 轮数、逃逸缺陷、人工介入次数 | P0 基线、P6 对比 |

## 7. 标杆验收

1. **事故回放**：v0.8.0 的每类失败（基线评审 §1）都有注入用例，在新机制下被机器拦住。例如提交真实快照被拒；filter-repo 或推 tag 被拒；重新注入 R17 未排序被测试抓住；声称已修但文件未变被证据检查拦住；去掉 R2 不降级保护后回放失败。
2. **覆盖**：六份规格的验收条目全部编号；可自动化的编号在 CI 中保证有对应测试，其余进入人工验收清单。
3. **同口径**：`verify` 一条命令，三处结果一致（云端跑 Python 部分，Swift 部分由 macOS CI 负责）。
4. **真实任务试跑**：下一个功能按新流程交付，与 v0.8.0 基线（基线评审 §2）对比。

## 8. 不做什么

- 不追求全自动：UI、真机导航与 R2 以上改动保留人审。
- 不改产品行为；只做为可测而必需的重构（如状态机抽纯函数）。
- 不上 Temporal、OpenTelemetry、合并队列、金丝雀发布。
- 不打 tag、不发版（AGENTS.md）。

## 9. 阶段

| 阶段 | 内容 | 退出标准 | 状态 |
|---|---|---|---|
| P0 基线 | 失败分类、基线指标、就绪度体检、可验证性地图；本方案入库 | 基线评审与本方案入库 | ✅ 2026-09-25 |
| P1 统一入口与证据 | `verify` 命令；修复证据脚本；PR 模板要求 CI 链接与证据；风险等级判定脚本 | 三处同口径；「声称已修」「自述代替 CI」两类注入被拦住 | ✅ 云端与 macOS CI 同口径；X1、X4 回放通过 |
| P2 结构护栏 | §5 服务端与 git 两层（ruleset 由用户按提供的配置操作）；Agent 层按工具补齐 | 回放「提交快照、改写历史、推 tag、改已有测试」全部被拒 | ✅ git 层与 Claude Code 实测；ruleset 待用户导入 |
| P3 判定器补强 | 属性测试、黄金快照、夹具、不降级回放、变异测试 | 注入 R2、R4、R11、R13、R17、R19 类 bug 全部被抓住；变异得分基线入库 | ✅ 19 个注入全部拦住；发现 H0925-1～5 |
| P4 规格可执行化 | 验收编号 ↔ 测试名映射与 CI 校验；任务与计划模板；模块风险等级 | 六份规格编号完成，映射检查在 CI 为绿 | ✅ 69 条编号，1 条登记缺口（DR14） |
| P5 独立评审 | 评审清单由失败分类生成；证据包格式；SwiftUI 截图回归（可选） | 评审清单覆盖回放集 | ✅ 截图回归暂缓（需稳定的 macOS 渲染基准） |
| P6 度量与试跑 | 事故回放集可一键运行；一个真实任务走完全流程 | 试跑数据与基线对比入库 | ⏳ 度量、质量棘轮与每周 quality 工作流就绪；试跑任务 [trial-001](trial-001-dr14.md) 待执行 |

每个阶段一个 PR，按 §4 的风险等级合并；改动 CI、hooks 或测试基础设施的阶段属于 R3。

## 10. 决定记录

| 日期 | 决定 |
|---|---|
| 2026-09-25 | 自治目标 L4：R0/R1 门禁全绿自动合并，其余每个 PR 人审证据包 |
| 2026-09-25 | 角色：Codex、Claude Code 做设计、评审与顾问；Zcode、OpenCode、Pi 做实现、测试与部署 |
| 2026-09-25 | 用户同意按提供的配置在仓库设置中启用 ruleset（`main` 与 `v*` tag 保护） |
| 2026-09-25 | D4：Agent 不自行合并 PR。R0/R1 由仓库 auto-merge 在门禁全绿后合并，R2 及以上由用户合并；Agent 层拒绝命令行与 MCP 的合并、开启自动合并工具 |

## 11. 待用户决定

| 编号 | 问题 | 选项 |
|---|---|---|
| H0925-4 | 过去日合并时，重扫总量更小但某一类更大，任务的三类合计与 model 明细不一致 | 按类别取较大值（合计可能高于旧报告）；或整任务沿用旧记录；或维持现状并在规范中写明 |
| H0925-6 | Zcode「前往会话」两份规格描述矛盾 | 确认 CLI 适配器与 App 入口各自的现役语义后统一表述 |
| 身份 | Agent 与用户共用 GitHub 身份，服务端无法区分谁在合并 | 给执行者单独身份（机器账号或 GitHub App）；或维持约定 |

## 12. 试跑规程

1. 用户指定执行方，把 [trial-001](trial-001-dr14.md) 交给它；执行方按任务说明实现并开 PR。
2. CI 的 harness job 自动生成风险等级、修复证据与交付度量；评审方按 [评审提示词](../templates/review-prompt.md) 评审。
3. 合并后在 trial-001 的「试跑记录」表中填入本次数据，与 v0.8.0 基线对比；两周内的逃逸缺陷一并记录。
4. 根据试跑暴露的问题调整 harness（规则、模板、检查），按 R3 走评审。

## 13. 未完成与后续工作

2026-09-25 PR #7 提交评审时的清单。完成一项就在「状态」列更新，不删除行。负责人：用户、设计评审方（Codex / Claude Code）、执行方（Zcode / OpenCode / Pi）。

### 13.1 上线前必须完成（否则对应护栏不存在）

| # | 事项 | 负责人 | 前置 | 状态 |
|---|---|---|---|---|
| A1 | 独立评审 PR #7（交接书：[harness-review-brief](../review/2026-09-25-harness-review-brief.md)） | 另一评审 Agent | — | 完成（[评审报告](../review/2026-09-26-harness-review.md)：首轮修改后可合并，PR7-R1..R6 已修；复评可合并，新增 PR7-R7..R9 已在本 PR 修复） |
| A2 | 合并 PR #7（R3，需用户批准） | 用户 | A1 | 待开始 |
| A4 | 导入 `.github/rulesets/main.json`、`release-tags.json`；新建 environment `release` 并设审批人（[harness 规范](../specs/delivery-harness.md) §5） | 用户 | — | 完成（2026-09-25，先于 A2 合并）：两条 ruleset 经 API 核对为 Active、规则与仓库文件一致、无绕过名单；environment `release` 经用户转贴的 API 输出核对：审批人为用户、未开禁止自审、管理员不可绕过、只允许 `v*` tag 部署；实际拦停由首次发版 V6 确认 |
| A3 | 本机装开发依赖（`requirements-dev.txt`）并 `python3 harness/git_guard.py install`；本机 macOS 首次跑 `bin/verify` | 用户 | A4 | 完成（2026-09-26）：本机 macOS `bin/verify --strict --full` 10 项全过，含 3 项 Swift 检查 |
| A5 | 每个执行方的环境（Zcode / OpenCode / Pi 所在机器或工作区）确认 git 守卫已安装 | 用户 | A3 | 完成（2026-09-26）：三个执行方都在本机同一克隆里工作，`core.hooksPath` 在仓库配置中，对其 worktree 同样生效；另起克隆需重新 install（`bin/verify` 会检查）。Pi 另需信任项目（规范 §5 第 5 步，待用户执行） |

### 13.2 待用户决定

| # | 事项 | 影响 | 状态 |
|---|---|---|---|
| D1 | H0925-4：过去日合并的口径（选项见 §11） | 决定后作为缺陷修复任务交执行方，并移除 `expectedFailure` 登记 | 待决定 |
| D2 | H0925-6：Zcode「前往会话」CLI 与 App 入口各自的现役语义 | 决定后统一两份规格与 ZN/IN14 验收 | 待决定 |
| D3 | 执行方是否使用单独的 GitHub 身份（机器账号或 GitHub App） | 决定是否能接上 L4 自动合并（E1） | 已决定（2026-09-26）：不另设身份，直接开启 R0/R1 自动合并；R2 以上由用户合并仍靠约定与 D4 守卫 |
| D4 | Agent 层是否一律拒绝 Agent 自己合并 PR（评审 PR7-R9 附带问题） | L4 下 R0/R1 由仓库 auto-merge 合并、R2 以上由用户合并，Agent 本身不需要合并权限；拒绝后「由用户合并」不再只靠约定 | 已决定：拒绝（§10），命令守卫已实现 |

### 13.3 首个试跑

| # | 事项 | 负责人 | 前置 | 状态 |
|---|---|---|---|---|
| T1 | 执行 [trial-001](trial-001-dr14.md)（补 DR14 测试），走完整流程 | 用户指定的执行方 | A2–A5 | 待开始 |
| T2 | 填写 trial-001 的试跑记录，与 v0.8.0 基线对比；两周后补逃逸缺陷 | 用户 + 评审方 | T1 | 待开始 |
| T3 | 根据试跑暴露的问题调整规则、模板或检查（R3） | 设计评审方 | T2 | 待开始 |

### 13.4 未验证的部分（需要真实环境）

| # | 事项 | 怎么验证 | 状态 |
|---|---|---|---|
| V1 | OpenCode 项目插件在真实 OpenCode 中加载并拦截 | 在本仓库启动 OpenCode，让它尝试 `git push --force`，应被拒绝 | 完成（2026-09-26）：真实 OpenCode 拒绝了设置覆盖变量的命令与编辑护栏文件（规范 §6） |
| V2 | Codex 用户级 PreToolUse 钩子（载荷格式、信任门禁） | 按 harness 规范 §5 第 4 步配置后同上 | 待验证 |
| V3 | Pi 扩展能否拦截工具调用 | 调研 Pi 扩展 API；可行则补适配器，不可行在规范中写明 | 完成（2026-09-26）：Pi 0.85 的 `tool_call` 事件可拦截，已补 `.pi/extensions/harness-guard.ts` 并在真实 Pi 中实测；扩展只在项目被信任后加载 |
| V7 | Zcode 工作区钩子在真实 Zcode 中拦截 | 用户按规范 §5 第 6 步信任后，让 Zcode 执行设置覆盖变量的命令与编辑 `harness/` 文件，应被拒绝 | 2026-09-26 CLI 实测：信任后仍放行（CLI 与 TUI 未打开工作区钩子，只有桌面版宿主打开）；桌面版待在 ZCode.app 里实测。在此之前，用 Zcode CLI 当执行方时没有 Agent 层，只靠 git 与服务端两层 |
| V4 | 写入器重构在真机上行为不变（W6 隐藏标题保留项目名；R8 进行中计数） | 用户真机抽查组件 | 待验证 |
| V5 | 每周 quality workflow 首次运行 | 合并后在 Actions 中手动触发一次 | 待 A2 |
| V6 | `release_check` 与发版审批在首次真实发版中生效 | 下次发版时确认 | 待发版 |

### 13.5 能力补齐（按价值排序）

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| E1 | 接上 L4 自动合并 | `.github/workflows/auto-merge.yml`：build 完成后由 `workflow_run` 触发，执行 main 上的定义与 `risk.py`，只合并 R0/R1、且只合并评估过的提交。用户 2026-09-26 决定不等单独身份（D3），直接开启。base_tests 同进程篡改检查的残余（评审 PR7-R7）仍在，R1 合并后抽样审计 | 已实现，待合并 |
| E2 | 暂缓回放项转为自动化 | `replay_cases.py` 的 `DEFERRED`：R7（经 generate_overview 的夹具）、R9（macOS 快照端到端）优先；R6 做成本机真实数据回放脚本（数据不入库）；X5 在 metrics 中按任务预算自动比对 CI 轮次 | 待开始 |
| E3 | 命令守卫改为按命令结构解析 | 目前按字符串匹配，已有 5 次误报，另有漏报面（变量间接展开、eval 拼接、wget 与脚本语言发起的请求；评审 PR7-R6、R9 已补去引号检查与不依赖参数顺序的 GitHub API 写请求）；改用 shlex 切分，只看真正执行的命令与参数 | 待开始（评审建议优先） |
| E4 | Swift 的「修复前失败」检查 | `evidence.py` 目前只对 Python 测试做退回重跑，Swift 引用只列出位置；在 macOS CI 中补上 | 待开始 |
| E5 | 扩大变异测试目标、提高报告合并得分 | 增加 `usage_cost`、`usage_report`；`_merge_day_tasks` 的 76% 中区分真实缺口与等价变异 | 待开始 |
| E6 | 熵治理：降低复杂度 | 20 个 C901 超标函数、`daily_report.py` 1693 行；每次顺带拆一处，棘轮自动收紧基线 | 持续 |
| E7 | SwiftUI 截图回归（P5 暂缓项） | 需要稳定的 macOS 渲染基准 | 暂缓 |
| E8 | `review_pack` 在大面积改规格时列出过多验收编号 | 只列真正被改动的行对应的编号 | 待开始 |
| E9 | GitHub Actions 的 Node 20 弃用警告 | 升级 `actions/checkout`、`setup-python` 的主版本 | 待开始 |
| E11 | 执行方角色首次真实运行 | 评审指出：本 PR 的实现方也是 Claude，「设计评审 / 执行」分工尚未实际走过；由 T1 首次验证，执行方用 OpenCode 或 Zcode | 待 T1 |
| E10 | 同步调研报告里的「落地交接说明」 | 报告（Claude 文档）中的交接节仍写着「从 P0 继续」，应改为指向本文 §13 | 待开始 |

